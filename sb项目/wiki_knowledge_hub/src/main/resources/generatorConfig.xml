<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE generatorConfiguration PUBLIC "-//mybatis.org//DTD MyBatis Generator Configuration 1.0//EN"
        "http://mybatis.org/dtd/mybatis-generator-config_1_0.dtd">
<generatorConfiguration>
    <!--引入外部配置 not support yaml-->
    <!--    <properties>application.yml</properties>-->

    <context id="default" defaultModelType="flat" targetRuntime="MyBatis3">
        <property name="beginningDelimiter" value="`"/>
        <property name="endingDelimiter" value="`"/>
        <property name="javaFileEncoding" value="UTF-8"/>

        <!--阻止注释的生成-->
        <commentGenerator>
            <property name="suppressAllComments" value="true"/>
            <property name="suppressDate" value="true"/>
            <property name="addRemarkComments" value="true"/>
        </commentGenerator>

        <!--数据库连接配置-->
        <!--        <jdbcConnection dirverClass="${spring.datasource.driverClassName}"
                                connectionURL="${spring.datasource.url}"
                                userId="${spring.datasource.username}"
                                password="${spring.datasource.password}">
                </jdbcConnection>-->
        <jdbcConnection driverClass="com.mysql.cj.jdbc.Driver"
                        connectionURL="jdbc:mysql://localhost:3306/wiki?characterEncoding=utf8&amp;useSSL=false&amp;serverTimezone=UTC&amp;rewriteBatchedStatements=true"
                        userId="wiki"
                        password="123456"/>

        <!--实体类自动生成配置-->
        <javaModelGenerator targetPackage="org.jinjianou.project.wiki_knowledge_hub.domain" targetProject="src/main/java">
            <property name="constructorBased" value="false"/>
            <property name="enableSubPackages" value="true"/>
            <!--if true field is final-->
            <property name="immutable" value="false"/>
            <property name="trimStrings" value="true"/>
        </javaModelGenerator>

        <!--mapper自动生成配置-->
        <sqlMapGenerator targetPackage="org.jinjianou.project.wiki.mapper" targetProject="src/main/resources">
            <!--            是否允许子包-->
            <property name="enableSubPackages" value="true"/>
        </sqlMapGenerator>

        <!--dao层接口自动生成配置-->
        <!--        采用xml的方式-->
        <javaClientGenerator type="XMLMAPPER" targetPackage="org.jinjianou.project.wiki_knowledge_hub.dao" targetProject="src/main/java">
            <property name="enableSubPackages" value="true"/>
        </javaClientGenerator>

        <!--指定数据表-->
        <!--        针对所有数据表自动生成代码 则可将值设为%-->
        <table tableName="ebook">
            <!--            sqlStatement：指定主键的查询语句，如果值为MySql，则使用SELECT LAST_INSERT_ID() 语句来查询主键-->
            <!--            identity：当值为true时，selectKey标签的order属性为AFTER-->
            <generatedKey column="id" sqlStatement="MySql" identity="true"/>
        </table>
    </context>
</generatorConfiguration>